Date = as.Date(TIMESTAMP_FIXED)
)
rlang::last_trace()
library(jsonlite)
library(data.table)
library(lubridate)
# --- caminho do ZIP ---
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# --- diretório temporário ---
tmp_dir <- tempdir()
# --- listar e extrair arquivos ---
zip_contents <- unzip(zip_file, list = TRUE)
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
unzip(zip_file, files = data_files, exdir = tmp_dir)
file_paths <- file.path(tmp_dir, data_files)
# --- função para extrair timestamps de UM arquivo ---
extract_dates <- function(file) {
ext <- tolower(tools::file_ext(file))
df <- tryCatch({
if (ext == "json") {
as.data.frame(fromJSON(file, flatten = TRUE))
} else {
fread(file, showProgress = FALSE)
}
}, error = function(e) return(NULL))
if (is.null(df)) return(NULL)
# possíveis colunas de tempo
time_cols <- intersect(
names(df),
c("TIMESTAMP", "timestamp", "gps_time", "time", "datetime", "date_time")
)
if (length(time_cols) == 0) return(NULL)
# tentar converter todas as colunas possíveis
dates <- lapply(time_cols, function(col) {
x <- df[[col]]
if (is.numeric(x)) {
as.Date(as.POSIXct(x, origin = "1970-01-01", tz = "UTC"))
} else {
suppressWarnings(as.Date(ymd_hms(x, tz = "UTC")))
}
})
unique(na.omit(unlist(dates)))
}
# --- aplicar a todos os arquivos ---
all_dates <- unique(unlist(lapply(file_paths, extract_dates)))
# --- resultado final ---
all_dates <- sort(all_dates)
print(all_dates)
library(stringr)
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# listar conteúdo do ZIP
zip_contents <- unzip(zip_file, list = TRUE)
# nomes dos arquivos
file_names <- zip_contents$Name
# função para extrair data do nome
extract_date_from_name <- function(x) {
# procurar YYYY_MM_DD ou YYYY-MM-DD
m <- str_extract(x, "(20[0-9]{2}[-_][0-9]{2}[-_][0-9]{2})")
if (is.na(m)) return(NA)
as.Date(gsub("_", "-", m))
}
# aplicar a todos os arquivos
dates <- as.Date(
na.omit(
sapply(file_names, extract_date_from_name)
)
)
# resultado final
dates <- sort(unique(dates))
print(dates)
library(jsonlite)
library(lubridate)
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
tmp_dir <- tempdir()
# listar arquivos no zip
zip_contents <- unzip(zip_file, list = TRUE)
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
# extrair
unzip(zip_file, files = data_files, exdir = tmp_dir)
file_paths <- file.path(tmp_dir, data_files)
# ---- função CORRETA para extrair data do SmartChamber ----
extract_date <- function(file) {
# ler como JSON (txt/dat também são JSON)
x <- tryCatch(fromJSON(file, simplifyVector = TRUE), error = function(e) NULL)
if (is.null(x)) return(NA)
# navegar pela estrutura
reps <- x[[1]]$reps
if (is.null(reps)) return(NA)
dates <- sapply(reps, function(r) {
# prioridade 1: header$Date
if (!is.null(r$header$Date)) {
return(as.Date(ymd_hms(r$header$Date)))
}
# fallback: header$gps_time
if (!is.null(r$header$gps_time)) {
return(as.Date(as.POSIXct(
as.numeric(r$header$gps_time),
origin = "1970-01-01", tz = "UTC"
)))
}
NA
})
unique(na.omit(dates))
}
# aplicar a todos os arquivos
all_dates <- unique(unlist(lapply(file_paths, extract_date)))
library(jsonlite)
library(lubridate)
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
tmp_dir <- tempdir()
# listar arquivos no zip
zip_contents <- unzip(zip_file, list = TRUE)
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
# extrair
unzip(zip_file, files = data_files, exdir = tmp_dir)
library(jsonlite)
library(lubridate)
extract_date <- function(file) {
x <- tryCatch(fromJSON(file, simplifyVector = FALSE),
error = function(e) return(NA))
# x precisa ser lista
if (!is.list(x) || length(x) == 0) return(NA)
root <- x[[1]]
# estrutura esperada
if (!is.list(root) || is.null(root$reps)) return(NA)
reps <- root$reps
dates <- sapply(reps, function(r) {
if (!is.list(r) || is.null(r$header)) return(NA)
# prioridade 1: header$Date (string)
if (!is.null(r$header$Date)) {
d <- suppressWarnings(ymd_hms(r$header$Date))
if (!is.na(d)) return(as.Date(d))
}
# fallback: gps_time (epoch)
if (!is.null(r$header$gps_time)) {
return(as.Date(as.POSIXct(
as.numeric(r$header$gps_time),
origin = "1970-01-01",
tz = "UTC"
)))
}
NA
})
unique(na.omit(dates))
}
# aplicar a todos os arquivos
all_dates <- unique(unlist(lapply(file_paths, extract_date)))
# resultado final
all_dates <- sort(all_dates)
print(all_dates)
all_dates <- as.Date(all_dates, origin = "1970-01-01")
print(all_dates)
# Caminho para o ZIP
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# Diretório temporário
tmp_dir <- tempdir()
# Listar conteúdo do ZIP
zip_contents <- unzip(zip_file, list = TRUE)
# Selecionar arquivos de interesse
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
# Extrair arquivos
unzip(zip_file, files = data_files, exdir = tmp_dir)
# Caminhos completos
file_paths <- file.path(tmp_dir, data_files)
# ---- Detectar JSON mal nomeado e renomear ----
for (f in file_paths) {
first_line <- tryCatch(readLines(f, n = 1, warn = FALSE), error = function(e) "")
if (grepl("^\\s*\\{", first_line)) {
file.rename(f, sub("\\.(txt|dat)$", ".json", f, ignore.case = TRUE))
}
}
# Listar todos os JSON reais
json_files <- list.files(
tmp_dir,
pattern = "\\.json$",
full.names = TRUE,
recursive = TRUE
)
# Ler com ffi_read_LIsmartchamber
dat_list <- lapply(json_files, function(f) {
tryCatch(
ffi_read_LIsmartchamber(f),
error = function(e) {
message("Skipped file: ", basename(f))
NULL
}
)
})
# Manter apenas leituras válidas
dat_list <- dat_list[
sapply(dat_list, function(x) !is.null(x) && nrow(x) > 0)
]
# Combinar tudo em uma única tabela
all_dat <- data.table::rbindlist(dat_list, fill = TRUE)
str(all_dat)
# Caminho para o ZIP
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# Diretório temporário
tmp_dir <- tempdir()
# Listar conteúdo do ZIP
zip_contents <- unzip(zip_file, list = TRUE)
# Selecionar arquivos de interesse
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
# Extrair arquivos
unzip(zip_file, files = data_files, exdir = tmp_dir)
# Caminhos completos
file_paths <- file.path(tmp_dir, data_files)
# ---- Detectar JSON mal nomeado e renomear ----
for (f in file_paths) {
first_line <- tryCatch(readLines(f, n = 1, warn = FALSE), error = function(e) "")
if (grepl("^\\s*\\{", first_line)) {
file.rename(f, sub("\\.(txt|dat)$", ".json", f, ignore.case = TRUE))
}
}
# Listar todos os JSON reais
json_files <- list.files(
tmp_dir,
pattern = "\\.json$",
full.names = TRUE,
recursive = TRUE
)
# Ler com ffi_read_LIsmartchamber
dat_list <- lapply(json_files, function(f) {
tryCatch(
ffi_read_LIsmartchamber(f),
error = function(e) {
message("Skipped file: ", basename(f))
NULL
}
)
})
# Manter apenas leituras válidas
dat_list <- dat_list[
sapply(dat_list, function(x) !is.null(x) && nrow(x) > 0)
]
# Combinar tudo em uma única tabela
all_dat <- data.table::rbindlist(dat_list, fill = TRUE)
View(all_dat)
library(jsonlite)
library(data.table)
library(lubridate)
# --- caminho do ZIP ---
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# --- diretório temporário ---
tmp_dir <- tempdir()
# --- listar e extrair arquivos ---
zip_contents <- unzip(zip_file, list = TRUE)
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
unzip(zip_file, files = data_files, exdir = tmp_dir)
file_paths <- file.path(tmp_dir, data_files)
# --- função para extrair timestamps de UM arquivo ---
extract_dates <- function(file) {
ext <- tolower(tools::file_ext(file))
df <- tryCatch({
if (ext == "json") {
as.data.frame(fromJSON(file, flatten = TRUE))
} else {
fread(file, showProgress = FALSE)
}
}, error = function(e) return(NULL))
if (is.null(df)) return(NULL)
# possíveis colunas de tempo
time_cols <- intersect(
names(df),
c("TIMESTAMP", "timestamp", "gps_time", "time", "datetime", "date_time")
)
if (length(time_cols) == 0) return(NULL)
# tentar converter todas as colunas possíveis
dates <- lapply(time_cols, function(col) {
x <- df[[col]]
if (is.numeric(x)) {
as.Date(as.POSIXct(x, origin = "1970-01-01", tz = "UTC"))
} else {
suppressWarnings(as.Date(ymd_hms(x, tz = "UTC")))
}
})
unique(na.omit(unlist(dates)))
}
# --- aplicar a todos os arquivos ---
all_dates <- unique(unlist(lapply(file_paths, extract_dates)))
# --- resultado final ---
all_dates <- sort(all_dates)
print(all_dates)
library(stringr)
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# listar conteúdo do ZIP
zip_contents <- unzip(zip_file, list = TRUE)
# nomes dos arquivos
file_names <- zip_contents$Name
# função para extrair data do nome
extract_date_from_name <- function(x) {
# procurar YYYY_MM_DD ou YYYY-MM-DD
m <- str_extract(x, "(20[0-9]{2}[-_][0-9]{2}[-_][0-9]{2})")
if (is.na(m)) return(NA)
as.Date(gsub("_", "-", m))
}
# aplicar a todos os arquivos
dates <- as.Date(
na.omit(
sapply(file_names, extract_date_from_name)
)
)
# resultado final
dates <- sort(unique(dates))
print(dates)
# Caminho para o ZIP
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
# Diretório temporário
tmp_dir <- tempdir()
# Listar conteúdo do ZIP
zip_contents <- unzip(zip_file, list = TRUE)
# Selecionar arquivos de interesse
data_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
# Caminho para o ZIP
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
# List files inside the zip
zip_contents <- unzip(zip_file, list = TRUE)
# Select SmartChamber files (json, txt, data)
raw_files <- zip_contents$Name[ grepl("\\.(json|txt|data)$", zip_contents$Name, ignore.case = TRUE) &
!grepl("manifest|test|generic", zip_contents$Name, ignore.case = TRUE)]
# Extract selected files
unzip(zip_file,
files = raw_files,
exdir = tmp_dir)
# List extracted files (REAL paths on disk)
file.list <- list.files(
path = tmp_dir,
pattern = "\\.(json|txt|data)$",
full.names = TRUE,
recursive = TRUE)
# Read ALL SmartChamber files (json, txt, data)
alldat1 <- lapply(file.list, function(f) {
tryCatch(
ffi_read_LIsmartchamber(f),
error = function(e) {
message("Skipped file: ", basename(f))
NULL} )})
# Read ALL SmartChamber files (json, txt, data)
alldat1 <- lapply(file.list, function(f) {
tryCatch(
ffi_read_LIsmartchamber(f),
error = function(e) {
message("Skipped file: ", basename(f))
NULL} )})
# Keep only successful reads with data
alldat1 <- alldat1[
sapply(alldat1, function(x) !is.null(x) && nrow(x) > 0)]
# Combine into a single table
alldat <- data.table::rbindlist(alldat1, fill = TRUE)
View(alldat)
sum(is.na(TIMESTAMP))
sum(is.na(all_datTIMESTAMP))
sum(is.na(all_dat$TIMESTAMP))
issuedf<-all_dat%>%filter(is.na(TIMESTAMP))
View(issuedf)
alldat_filtered<-alldat%>%filter(!is.na(TIMESTAMP))
test<-alldat_filtered%>%mutate(Date=as.Date(TIMESTAMP))
unique(test$Date)
library(fluxfinder)
library(data.table)
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
tmp_dir  <- tempdir()
# listar arquivos no zip
zip_contents <- unzip(zip_file, list = TRUE)
raw_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
# extrair
unzip(zip_file, files = raw_files, exdir = tmp_dir)
# caminhos completos
file_paths <- file.path(tmp_dir, raw_files)
# ---- FIX CRÍTICO: renomear JSON disfarçado ----
for (f in file_paths) {
first_line <- tryCatch(readLines(f, n = 1, warn = FALSE), error = function(e) "")
if (grepl("^\\s*\\{", first_line)) {
new_name <- sub("\\.(txt|dat)$", ".json", f, ignore.case = TRUE)
if (new_name != f) file.rename(f, new_name)
}
}
# agora listar APENAS json reais
json_files <- list.files(tmp_dir, pattern = "\\.json$", full.names = TRUE, recursive = TRUE)
# ler com ffi
alldat1 <- lapply(json_files, function(f) {
tryCatch(
ffi_read_LIsmartchamber(f),
error = function(e) NULL
)
})
# manter só leituras válidas
alldat1 <- alldat1[sapply(alldat1, function(x) !is.null(x) && nrow(x) > 0)]
# combinar
alldat <- data.table::rbindlist(alldat1, fill = TRUE)
test<-alldat_filtered%>%mutate(Date=as.Date(TIMESTAMP))
unique(test$Date)
library(fluxfinder)
library(data.table)
library(dplyr)
library(lubridate)
# -------------------------------
# 1) Caminho do ZIP e temp dir
# -------------------------------
zip_file <- "D:/GitHub/LE-Synoptic-discrete-data/Soil_Fluxes_Synoptic_LE/2024/Raw Data_LE_2024.zip"
stopifnot(file.exists(zip_file))
tmp_dir <- tempdir()
# -------------------------------
# 2) Extrair arquivos do ZIP
# -------------------------------
zip_contents <- unzip(zip_file, list = TRUE)
raw_files <- zip_contents$Name[
grepl("\\.(json|txt|dat)$", zip_contents$Name, ignore.case = TRUE)
]
unzip(zip_file, files = raw_files, exdir = tmp_dir)
file_paths <- file.path(tmp_dir, raw_files)
# -------------------------------
# 3) Renomear JSON disfarçado (.txt / .dat -> .json)
# -------------------------------
for (f in file_paths) {
first_line <- tryCatch(readLines(f, n = 1, warn = FALSE), error = function(e) "")
if (grepl("^\\s*\\{", first_line)) {
new_name <- sub("\\.(txt|dat)$", ".json", f, ignore.case = TRUE)
if (new_name != f) file.rename(f, new_name)
}
}
# -------------------------------
# 4) Ler TODOS os SmartChamber com ffi
# -------------------------------
json_files <- list.files(tmp_dir, pattern = "\\.json$", full.names = TRUE, recursive = TRUE)
alldat_list <- lapply(json_files, function(f) {
tryCatch(
ffi_read_LIsmartchamber(f),
error = function(e) NULL
)
})
# manter só leituras válidas
alldat_list <- alldat_list[
sapply(alldat_list, function(x) !is.null(x) && nrow(x) > 0)
]
# bind final
alldat <- data.table::rbindlist(alldat_list, fill = TRUE)
# -------------------------------
# 5) RECONSTRUIR timestamp corretamente
# -------------------------------
alldat <- alldat %>%
mutate(
TIMESTAMP_FIXED = case_when(
# JSON mais novos
!is.na(TIMESTAMP) ~ as.POSIXct(TIMESTAMP, tz = "UTC"),
library(dplyr)
library(ggplot2)
library(ggpubr)
library(stringr)
library(readxl)
library(tidyr)
library(tibble)
library(openxlsx)
library(gt)
library(kableExtra)
library(magick)
library(webshot2)
#read in 2022 Dionex Data:
dat22 <- read.csv("2022/COMPASS_SynopticLE_Soil_Fluxes_2022.csv")
# head(dat22)
#read in 2023 Dionex Data:
dat23 <- read.csv("2023/COMPASS_SynopticLE_Soil_Fluxes_2023.csv")%>%
rename(Plot_ID=Plot)
# head(dat23)
all_dat <- bind_rows(dat22, dat23)
all_dat$X <- NULL
sum(is.na(all_dat$Site))
sum(is.na(all_dat$Zone))
sum(is.na(all_dat$TIMESTAMP))
sum(is.na(all_dat$Replicate))
sum(is.na(all_dat$RepNumber))
df_norepl<-alldat%>%filter(is.na(Replicate))
df_norepl<-all_dat%>%filter(is.na(Replicate))
View(df_norepl)
#read in 2022 Dionex Data:
dat22 <- read.csv("2022/COMPASS_SynopticLE_Soil_Fluxes_2022.csv")
# head(dat22)
#read in 2023 Dionex Data:
dat23 <- read.csv("2023/COMPASS_SynopticLE_Soil_Fluxes_2023.csv")%>%
rename(Plot_ID=Plot)
# head(dat23)
all_dat <- bind_rows(dat22, dat23)
all_dat$X <- NULL
sum(is.na(all_dat$Replicate))
df_norepl<-all_dat%>%filter(is.na(Replicate))
all_dat_filtred<- all_dat%>%filter(!is.na(Replicate))
1685-1674
